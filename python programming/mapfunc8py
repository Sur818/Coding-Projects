import math
def func(a,b):
	return int(math.pow(a,b))

n=int(input("how many data want to enter:-"))
l=[int(input(f"l[{i+1}]=")) for i in range(n)]
l1=[i for i in range(1,n+1)]
print(list(map(func,l,l1)))
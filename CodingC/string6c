#include<stdio.h>
int main()
{
 int i,j,n;
char str[100];
printf("enter string");
gets(str);
n=strlen(str);
for(i=0;i<n;i++)
{
if(str[i]==str[i+1])
{
for(j=i;j<n;j++)
{
 str[j]=str[j+1];
}
n--;
i--;
}
}
puts(str);
return 0;
}

 